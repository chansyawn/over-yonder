[package]
name = "continue-desktop"
version = "0.0.0"
description = "Continue demo desktop app"
authors = ["Continue"]
edition = "2021"

[lib]
name = "continue_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-fs = "2"
